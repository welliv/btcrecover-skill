# ROADMAP — The Next Spiral

Each arc builds on all previous arcs. The pattern grows outward.

## F(1) — Complete (this session)
All nine Fibonacci redesign tasks from the first-principles analysis.
- Resource Map added to SKILL.md
- Three redundant files removed
- python3 standardised across all sub-skills
- Feedback loop added to sweep-reminder.sh
- benchmark scripts audited

## F(2) — Near term
Second entry in references/hybrid-recovery.md.
This must come from a real recovery — not from design.
When the first user reports a pattern not in the library, it goes here.
The second spiral arm grows from use.

## F(3) — Short term
GPG commit signing.
Makes VERIFIED.md's "check the Verified badge" actually verifiable.
Without this, the authenticity chain is aspirational. With it, it's real.

## F(5) — Medium term
AddressDB integration into the feasibility gate.
A user with an AddressDB file changes the feasibility calculation for
seed recovery — more missing words become recoverable.
The feasibility table in SKILL.md should detect this case.

## F(8) — Longer term
Multi-language seed recovery documentation.
btcrecover supports non-English BIP39 wordlists (Japanese, Spanish,
Chinese). The seed sub-skill currently documents English only.
Each new language is a new spiral arm growing from the same centre.

---

The spiral does not stop here. Each completed arc reveals the next.
